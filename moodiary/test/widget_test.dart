import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder: full widget tests need ProviderScope + Hive init.
    // Add integration tests once the test harness is configured.
    expect(true, isTrue);
  });
}
